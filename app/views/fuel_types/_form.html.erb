<div class="container">
    <%= form_with(model: fuel_type, local: true) do |form| %>
    <% if fuel_type.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(fuel_type.errors.count, "error") %> prohibited this fuel_type from being saved:</h2>
  
        <ul>
        <% fuel_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
      <div class="col-md-8 offset-md-2">

          <div class="card" style="padding: 5vh; background-color:#0000008a;">
  
              <div class="form-group row">
                  <%= form.label :fuelname, class:"col-sm-2 col-form-label" %>
                  <div class="col-sm-10">
                      <%= form.text_field :fuelname,class:"form-control" %>
                  </div>
                </div>
              
                <div class="form-group row">
                  <%= form.label :price, class:"col-sm-2 col-form-label" %>
                  <div class="col-sm-10">
                      <%= form.number_field :price, class:"form-control" %>
                  </div>
                </div>
              
                <div class="actions">
                  <%= form.submit 'Add',class:'btn btn-secondary btn-block' %>
                </div>
                </div>
      </div>
  <% end %>
  
</div>